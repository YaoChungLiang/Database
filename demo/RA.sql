-- Ted Codd
-- SQL to Relational algebra : A Relational model of Data for Large Shared Data Banks
-- While processing your querries, RDBMS will store an RA tree
-- sigma <= WHERE (selection)
-- pi <= SELECT (projection)
-- U <= Union
-- X <= Cartesian Product

-- RA tree bottom -> Data sources
--         Top -> Query output
